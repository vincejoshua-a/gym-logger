# Copy to .env.local and fill in. .env.local is git-ignored.
# The Apps Script web app URL (ends in /exec):
VITE_SHEET_ENDPOINT=
# The secret token that matches SECRET in google-apps-script/Code.gs:
VITE_SHEET_TOKEN=
